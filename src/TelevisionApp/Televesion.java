package TelevisionApp;

import static sun.security.ssl.SSLLogger.isOn;

public class Televesion<mode> {

    private boolean mode;
    private String name;
    private int channelNumber = 1;
    private int volume = 30;
    private boolean isOn = true;


    private final boolean isOff;

    {
        isOff = false;
    }

    public Televesion(String tvStation) {

        String sonyTv = "boolean";
        String name = "boolean",
                tvName = sonyTv;
        this.channelNumber = channelNumber;
    }

    public void setTurnOff(String aFalse) {
        setTurnOff("false");
    }

    private void getChannelNumber() {
        this.mode = mode;
        mode = true;

        if (channelNumber <= 30)
            new StringBuilder().append(channelNumber).append("UserInput").toString();
        return;
    }

    public void setChannelNumber(int channelNumber) {
        this.setChannelNumber();
        channelNumber = channelNumber;

        int channel = 0;
        if (isOn & channel > 0) {
            this.channelNumber = channel;
            boolean checkIsOn;
        } else {
            boolean checkIsOn;
        }

        {
            if (volume <= 30) {
                volume++;
            } else {
                volume = 30;
            }

        }

        int increaseVolume; {
            increaseVolumeNow();
            volume += 1;


            return ;
        }
    }

    private void setChannelNumber() {
    }

    private void increaseVolumeNow() {
    }
}








